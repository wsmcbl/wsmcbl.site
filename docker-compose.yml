services:
  site:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    container_name: site
    networks:
      - app-network

networks:
  app-network:
    external: true
